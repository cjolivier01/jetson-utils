// Simple test to ensure HIP-built kernels compile & link.
// Does not run any GPU kernels; validates build integration only.

int main() {
    // If we got here, the HIP lib linked successfully.
    return 0;
}
